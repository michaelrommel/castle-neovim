// functions1.rs
// Execute `rustlings hint functions1` or use the `hint` watch subcommand for a hint.

mod mymodule;

fn call_me() {
    print!("Hello, world!");
}

fn plus(x: i32) -> i32 {
    x + 1;
}

asdf;

fn main() {
    call_me();
    call_me_not()
}
